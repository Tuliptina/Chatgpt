# 4o with Memory v7.0 — Streamlit Client
# Includes embedded Mnemo engine (SQLite + FAISS + NumPy)

streamlit
requests
httpx
nest_asyncio
pypdf
tiktoken
python-docx
huggingface_hub>=0.20.0

# v7.0: Local Mnemo engine dependencies (for Option D embedded mode)
# CPU-only torch to avoid ~2GB CUDA download
--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple
torch
sentence-transformers>=2.2.2
faiss-cpu>=1.7.0
numpy>=1.24.0
